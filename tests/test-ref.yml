name: Test ref input (URI reference of the schema)
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - id: action
        uses: .
        with:
          values: tests/fixtures/values-ref.yaml
          output: tests/fixtures/values-ref.schema.json
          draft: 2020
          ref: 'https://example.com/base-schema.json'

      - name: Assert plugin-path output is set
        env:
          PLUGIN_PATH: ${{ steps.action.outputs.plugin-path }}
        run: |
          echo "plugin-path: $PLUGIN_PATH"
          if [ -z "$PLUGIN_PATH" ]; then
            echo "FAIL: plugin-path output is empty"
            exit 1
          fi
          echo "PASS: plugin-path is set"

      - name: Assert output schema file was generated
        run: |
          if [ ! -f "tests/fixtures/values-ref.schema.json" ]; then
            echo "FAIL: values-ref.schema.json was not generated"
            exit 1
          fi
          echo "PASS: schema file exists"
          cat tests/fixtures/values-ref.schema.json

      - name: Assert schema is valid JSON
        run: |
          SCHEMA=$(cat tests/fixtures/values-ref.schema.json)
          if ! echo "$SCHEMA" | python3 -m json.tool > /dev/null 2>&1; then
            echo "FAIL: schema is not valid JSON"
            exit 1
          fi
          echo "PASS: schema is valid JSON"

      - name: Assert schema contains ref URI
        run: |
          SCHEMA=$(cat tests/fixtures/values-ref.schema.json)
          echo "Schema: $SCHEMA"
          if ! echo "$SCHEMA" | grep -q 'https://example.com/base-schema.json'; then
            echo "FAIL: schema does not contain the expected ref URI"
            exit 1
          fi
          echo "PASS: schema contains expected ref URI"
